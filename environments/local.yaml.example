domain: __DOMAIN__

apps:
  voiceIngress:
    enabled: __VOICE_ENABLED__
  # gifbox:
  #   enabled: true
  #   provider: upstream  # upstream (Tenor) | klipy | custom
  #   apiKey: ""          # Tenor or Klipy API key
  client:
    enableVideo: __VIDEO_ENABLED__

livekit:
  enabled: __VOICE_ENABLED__

# SMTP (disabled by default — no email verification)
smtp:
  host: ""
  port: 25
  username: ""
  password: ""
  fromAddress: "noreply@__DOMAIN__"
  replyTo: ""
  useTls: false
  useStarttls: false

# Generate with: openssl rand -hex 24
secretSeed: ""
